# POST Cancel Guest Admin

Endpoint used on the admin portal to cancel a guest order.

| verb       | path                          |
| ---------- | ----------------------------- |
| **`POST`** | /orders/:orderId/cancel-guest |
