# GET Cancel Guest By Token

Cancels a guest order with a valid cancellation token.

| verb      | path                              |
| --------- | --------------------------------- |
| **`GET`** | /orders/cancel-guest-token/:token |
